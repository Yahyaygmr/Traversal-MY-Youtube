

<div class="leave-comment-form mt-5" id="reply">
    <h4 class="side-title mb-2">Yorum Yap</h4>
    @if ((ViewBag.UserId) != null)
    {
        <p class="mb-4">
            İşaretli Alanların Doldurulmazsı Zorunludur *
        </p>

        <form asp-action="AddComment" asp-controller="Comment" method="post">

            <div class="form-group">
                <textarea name="CommentContent" class="form-control" placeholder="Yorumunuz*" required=""
                          spellcheck="false"></textarea>
            </div>
            <div class="input-grids row">
                @*
            <div class="form-group col-lg-6">
            <input type="text" name="CommentUser" class="form-control" placeholder="Adınız*"
            required="">
            </div> *@
                <div class="form-group col-lg-6">
                    <input type="text" name="DestinationId" class="form-control" value="@ViewBag.Id" placeholder="DEstinasyonId*" required="" hidden>
                    <input type="text" name="AppUserId" class="form-control" value="@ViewBag.UserId" placeholder="AppUserId*" required="" hidden>
                    <div class="submit text-right">
                        <button class="btn btn-style btn-primary">Yorum Yap </button>
                    </div>
                </div>
            </div>

        </form>
    }
    else
    {
        <span>Yorum yapabilmek için lütfen <a href="/Login/SignIn/">kullanıcı girişi</a>  yapın.</span>
    }

</div>